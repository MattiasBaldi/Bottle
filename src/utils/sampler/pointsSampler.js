import { MeshSurfaceSampler } from "three/examples/jsm/Addons.js";

/**
 * Utility class to create an THREE.Points with points positioned on a surface using MeshSurfaceSampler.
 */

export default class pointsSampler {
    constructor() 
    {
        
    }
}